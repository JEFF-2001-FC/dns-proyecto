"use client";
import { useState } from "react";
import { uploadEventResource } from "../actions";
export function EventResourceForm({ eventId }: { eventId: string }) { const [kind, setKind] = useState("file"); return <details className="mt-3 rounded-xl border border-line bg-white p-3"><summary className="cursor-pointer text-sm font-semibold">Adjuntar material al evento</summary><form action={uploadEventResource.bind(null, eventId)} className="mt-3 space-y-2"><input name="title" required placeholder="Título del recurso" className="w-full rounded-xl border border-line px-3 py-2 text-sm" /><select name="kind" value={kind} onChange={(event) => setKind(event.target.value)} className="w-full rounded-xl border border-line px-3 py-2 text-sm"><option value="file">Archivo PDF</option><option value="link">Enlace (Drive, etc.)</option><option value="note">Nota / indicaciones</option></select>{kind === "file" && <input name="file" type="file" accept="application/pdf" required className="w-full rounded-xl border border-line px-3 py-2 text-sm" />}{kind === "link" && <input name="externalUrl" type="url" required placeholder="https://drive.google.com/..." className="w-full rounded-xl border border-line px-3 py-2 text-sm" />}{kind === "note" && <textarea name="body" required rows={3} placeholder="Indicaciones para los líderes" className="w-full rounded-xl border border-line px-3 py-2 text-sm" />}<button className="rounded-xl bg-ink px-3 py-2 text-sm font-semibold text-white">Guardar recurso</button></form></details>; }
